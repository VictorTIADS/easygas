<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorFundoSplash"
    tools:context=".SuccessfulResisterActivity">

    <include layout="@layout/mastertoolbar"/>

    <ImageView
        android:id="@+id/icon_state"
        android:layout_width="154dp"
        android:layout_height="170dp"
        android:src="@drawable/ic_check_circle_black_24dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toTopOf="@id/lbl_state"
        app:layout_constraintTop_toBottomOf="@id/master_tool_bar" />

    <TextView
        android:id="@+id/lbl_state"
        app:layout_constraintTop_toBottomOf="@id/icon_state"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        android:layout_width="wrap_content"
        android:layout_marginTop="15dp"
        android:textSize="16sp"
        android:layout_marginBottom="110dp"
        android:layout_height="wrap_content"
        android:text="Usuário Cadastrado com Sucesso"
        android:textColor="@color/colorWhite"
         />

</androidx.constraintlayout.widget.ConstraintLayout>